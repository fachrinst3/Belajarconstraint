<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="format" parent="android:Widget.DeviceDefault.Light.TextView">
        <item name="android:layout_width">212dp</item>
        <item name="android:layout_height">28dp</item>
        <item name="android:layout_marginStart">16dp</item>
        <item name="android:layout_marginTop">44dp</item>
        <item name="android:fontFamily">@font/archivo_narrow_bold</item>
        <item name="android:textSize">18sp</item>
    </style>
</resources>
